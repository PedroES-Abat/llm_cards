[project]
name = "llm-invoicing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flasgger>=0.9.7.1",
    "flask>=3.1.3",
    "flask-restx>=1.3.2",
    "streamlit>=1.56.0",
]

[tool.uv.workspace]
members = [
    "llm_invoicing",
]
